# Godot 4
.godot/
*.translation
*.app
.mono/
data_*/
*.uid

# C++ / vcpkg build trees (keep committed GDExtension binary under bin/ optional)
build/
cmake-build-*/
_deps/
vcpkg_installed/

# Local C++ / GDExtension build outputs
build/
bin/
*.gd.uid
*.gdextension.uid

# Mobile export artifacts (export_presets.cfg IS committed — no secrets in repo presets)
exports/
android/
android/build/
android/.build_version
*.apk
*.aab
*.ipa
*.xcarchive
