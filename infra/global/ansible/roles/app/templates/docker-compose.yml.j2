services:
  backend:
    image: ghcr.io/acfharbinger/mobile-fortress-backend:{{ backend_version | default('latest') }}
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: "{{ database_url }}"
