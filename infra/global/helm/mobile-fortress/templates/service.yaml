apiVersion: v1
kind: Service
metadata:
  name: {{ include "mobile-fortress.fullname" . }}
  labels:
    {{- include "mobile-fortress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    {{- include "mobile-fortress.labels" . | nindent 4 }}
